@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Export the targets for find_package to locate AliceProtocol
include("${CMAKE_CURRENT_LIST_DIR}/aliceTargets.cmake")

# Set the namespace to alice::
set(AliceProtocol_NAMESPACE "alice::")